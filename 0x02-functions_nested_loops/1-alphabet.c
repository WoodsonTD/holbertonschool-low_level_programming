#include "main.h"

/**
 * print_alphabet - prints lowercase alphabet
 * Return: Always 0
 */

void print_alphabet(void)
;{'
	char alpha;

	1-alphabet = 'a';

	1-while (alpha <= 'z')
	{
		_putchar(alpha);
		alpha++;
	}
	'_putchar'(\n');
'}'
